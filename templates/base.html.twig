<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⌚</text></svg>">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.scss') }}">
        {% endblock %}

        {% block javascripts %}
            <script src="{{ asset('js/base.js') }}"></script>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </head>
    <body>
        <nav class="navbar {% if app.request.attributes.get('_route') == 'app_home' %}navbar-transparent{% endif %}">
            <a href="{{ path('app_home') }}" class="logo">LuxWatch</a>
            <div class="nav-links">
                <a href="{{ path('app_home') }}">Accueil</a>
                <a href="{{ path('app_collection') }}">Collection</a>
                <a href="{{ path('app_particuliers') }}">Particuliers</a>
                <a href="#">Contact</a>
            </div>
            <div class="nav-icons">
                <div class="search-container">
                    <input type="text" class="search-input" placeholder="Rechercher...">
                    <a href="#" class="search-icon"><i class="fas fa-search"></i></a>
                </div>
                {% if app.user %}
                    <a href="#" class="icon-link"><i class="fas fa-heart"></i></a>
                    <a href="{{ path('app_cart') }}" class="icon-link"><i class="fas fa-shopping-cart"></i></a>
                    <a href="#" class="icon-link"><i class="fas fa-user"></i></a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="login-button">Se connecter</a>
                {% endif %}
            </div>
        </nav>

        {% block body %}{% endblock %}

        <footer class="footer">
            <div class="footer-content">
                <div class="footer-section">
                    <h3 class="footer-title">À propos</h3>
                    <p class="footer-text">LuxWatch - Votre destination pour les montres de luxe</p>
                </div>
                
                <div class="footer-section">
                    <h3 class="footer-title">Contact</h3>
                    <a href="mailto:contact@luxwatch.com" class="footer-contact">Email: contact@luxwatch.com</a>
                    <a href="tel:+33123456789" class="footer-contact">Tél: +33 1 23 45 67 89</a>
                </div>
            </div>
            <div class="footer-bottom">
                <p>© 2024 LuxWatch. Tous droits réservés.</p>
            </div>
        </footer>
    </body>
</html>
